import { Alignment } from '@/types/layout';

export const DEFAULT_ALIGNMENT_SETTINGS: Required<Alignment> = {
  horizontalAlignment: 'center',
  horizontalOffset: 0,
  verticalAlignment: 'center',
  verticalOffset: 0
};

export const DEFAULT_FOCUS_SCALE_MULTIPLIER = 2;
