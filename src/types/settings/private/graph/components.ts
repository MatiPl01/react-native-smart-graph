import { EdgeSettings } from '@/types/settings/public/graph/components';
import { DeepRequiredAll } from '@/types/utils';

/*
 * DEFAULT SETTINGS
 */
export type AllEdgeSettings = DeepRequiredAll<EdgeSettings>;

/*
 * INTERNAL SETTINGS
 */
