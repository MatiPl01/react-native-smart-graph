import { AllGraphViewSettings } from '@/types/settings';

export const DEFAULT_VIEW_SETTINGS: AllGraphViewSettings = {
  autoSizingTimeout: 2000,
  initialScale: 1,
  objectFit: 'none',
  padding: 0,
  scales: [0.25, 1, 2, 4]
};
