import { RandomLayoutType } from '@/types/placement';

export const SHARED = {
  vertexRadius: 20,
  minVertexDistance: 20
};

export const RANDOM_PLACEMENT = {
  layoutType: 'honeycomb' as RandomLayoutType,
  density: 0.5
};
